/**
 * http://usejsdoc.org/
 */
 
 // Receives incoming client connections
 function receiveConnection() {
 }
 
 // disconnects client at game termination
 function endConnection() {
	 
 }
 
 // server generates UUID and returns it to requesting client
 function returnUUID() {
	 
 }
 
 // Adds requesting client to wait lobby is wait lobby exists
 function clientJoin() {
	 
 }
 
 // If no wait lobby exists for initiating client to join,
 // creates wait lobby and places requesting client in lobby
 function createLobby() {
	 
 }
 
 
 